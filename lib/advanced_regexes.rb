# Implement the below functions in order.
module AdvancedRegexes
  # Returns a regex that matches the string "hello" case-insensitively.
  def matching_hello_case_insensitively
    /WRITE ME/
  end

  # Returns a regex that matches the string "hello" case-insensitively at the
  # end of a string.
  def ends_with_hello
    /WRITE ME/
  end

  # Returns a regex that matches any string that has some repeated string in it.
  #
  # Examples that should match: "something asdf asdf", "abc something abc".
  def say_it_again
    /WRITE ME/
  end
end
